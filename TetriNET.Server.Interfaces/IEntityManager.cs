using System.Collections.Generic;
using TetriNET.Common.Contracts;

namespace TetriNET.Server.Interfaces
{
    //public interface IEntityManager<T> where T:IEntity
    //{
    //    bool Add(T entity);
    //    bool Remove(T entity);
    //    void Clear();

    //    int MaxEntities { get; }
    //    int EntityCount { get; }
    //    object LockObject { get; }

    //    int FirstAvailableId { get; }

    //    List<T> Entities { get; }

    //    T this[string name] { get; }
    //    T this[int id] { get; }
    //    T this[ITetriNETCallback callback] { get; }
    //}
}
