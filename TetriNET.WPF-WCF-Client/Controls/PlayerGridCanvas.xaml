<UserControl x:Class="TetriNET.WPF_WCF_Client.Controls.PlayerGridCanvas"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d"
             DataContext="{Binding RelativeSource={RelativeSource Self}}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="16"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center"  MaxWidth="192">
            <TextBlock Text="{Binding DisplayPlayerId}" Visibility="{Binding PlayerIdVisibility}"/>
            <TextBlock Text="." Visibility="{Binding PlayerIdVisibility}" Margin="0,0,2,0"/>
            <TextBlock Text="{Binding PlayerName}" />
        </StackPanel>
        <Border Grid.Row="1" Background="Green">
            <Canvas Background="Black" x:Name="Canvas" VerticalAlignment="Top" HorizontalAlignment="Left" Width="192" Height="352">
                <!-- will be filled with controls in ctor -->
            </Canvas>
        </Border>
    </Grid>
</UserControl>
